/** Dealer draws to 17 and STANDS on soft 17 (S17 — REBUILD_SPEC §4). */
export const DEALER_STAND_TOTAL = 17;

/** Anti-spam cooldown between player actions (~0.6s — REBUILD_SPEC §4). UI-enforced later. */
export const ACTION_COOLDOWN_MS = 600;

/** One split maximum, no resplitting (REBUILD_SPEC §4). */
export const MAX_SPLITS = 1;
