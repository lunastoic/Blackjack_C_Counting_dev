/** Phone-first layout constants. */
export const layout = {
  /** Minimum touch target per accessibility guidance. */
  touchTarget: 44,
  /** Max content width so large phones don't stretch content edge-to-edge. */
  maxContentWidth: 560,
  /** Default horizontal screen padding. */
  screenPaddingH: 20,
  /** Progression header height (excluding safe-area inset). */
  headerHeight: 56,
} as const;
